import { createStore } from 'vuex'
import Products from './modules/product'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Products : Products
  }
})
